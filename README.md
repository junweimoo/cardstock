# imgengine

Static image composition engine — C++ core, Python API. M0 skeleton.

## Develop
```bash
uv venv && source .venv/bin/activate
uv pip install -e . --no-build-isolation
pytest
python examples/solid_card.py
```
